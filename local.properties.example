# Copy this file to `local.properties` and fill in your values.
# `local.properties` is gitignored — never commit real secrets.
# Every key here can also be supplied as an environment variable.

# aisstream.io API key (required). Get one at https://aisstream.io/.
# Used by the area-monitoring streams.
AIS_API_KEY=your_aisstream_api_key_here

# RECOMMENDED: three keys from THREE SEPARATE aisstream accounts.
# aisstream limits concurrent connections PER ACCOUNT, not per key. The app opens
# three independent kinds of stream — area monitoring (AIS_API_KEY), followed
# ships (FOLLOW_AIS_API_KEY) and the coverage map (HEATMAP_AIS_API_KEY). If two of
# them share an account they fight over the same connection slot and one keeps
# getting rejected (429 loop / 1006 close). Give each a key from its own account.

# Key for the followed-ships stream. Empty = reuse AIS_API_KEY (works, but the
# follow handshake may hit a per-account 429 loop against the area streams).
FOLLOW_AIS_API_KEY=

# Key for the worldwide Coverage map stream. MUST be from a separate account.
# Empty = the coverage map feature stays off. Write it bare — no inline comment.
HEATMAP_AIS_API_KEY=

# Initial bounding-box preset. Must match a key defined in `bounding-boxes.json`
# (default keys: bari | nord_adriatico | taranto | puglia).
BBOX_PRESET=bari

# Enrich ship details by scraping VesselFinder / MarineTraffic (true | false).
IMPORT_VF_DATA=false
IMPORT_MT_DATA=false

# Screen ships against the OFAC SDN sanctions list (downloaded, matched by
# IMO/name/call sign). A match is a very strong risk signal (true | false).
IMPORT_SANCTIONS=false

# Additional sanctions lists on top of OFAC: EU consolidated, UK OFSI and UN
# designated vessels (via OpenSanctions, downloaded daily). Only effective while
# IMPORT_SANCTIONS is on. Default on — set to false to screen against OFAC only.
IMPORT_SANCTIONS_EXTRA=true

# Equasis ownership lookup (true | false). On-demand only: enables the "Recupera
# informazioni Equasis" button in the ship detail to fetch registered owner / ISM
# manager / operator by IMO. Never runs automatically. Needs a free Equasis
# account (register at https://www.equasis.org/) set below.
IMPORT_EQUASIS=false
EQUASIS_USER=
EQUASIS_PASSWORD=

# Port State Control screening (true | false): Paris/Tokyo MoU flag-performance
# lists (white/grey/black, bundled, updated yearly) + Paris MoU banned-ships list
# (downloaded daily). Banned = refusal of access after repeated detentions.
IMPORT_PSC=false

# Global Fishing Watch enrichment (true | false). Default ON. Adds GFW vessel
# identity + behavioural events (encounters, loitering, port visits, AIS-off
# "gaps") to the ship detail and the risk score. Needs an API token (NOT the
# website login): generate one at https://globalfishingwatch.org/our-apis/ and
# paste it below. Free tier is non-commercial use only. Without a token the
# feature stays inert (the detail panel shows a hint).
IMPORT_GFW=true
GLOBAL_FISHING_WATCH_TOKEN=

# Telegram bot notifications (optional). A single bot serves all users; each
# user links their chat from the app Settings → Telegram tab (a /start <code>
# flow). Create a bot with @BotFather on Telegram and paste its token below.
# Empty = the bot stays off. The token is a SECRET — never commit it. No public
# URL / webhook needed: the app long-polls Telegram. Notifications are sent in
# each user's own language; per-category toggles live in the Settings UI.
TELEGRAM_BOT_TOKEN=

# ── Authentication (multi-user) ──────────────────────────────────────────────
# The app is gated by a session login. Users register (name, surname, email,
# password) and must be approved by an administrator before they can sign in.
# Each user has their own areas, settings, flagged + followed ships.
#
# A built-in administrator is always (re)seeded at startup if missing. Override
# its credentials here; the password defaults to the shipped value if unset.
# Login accepts the username OR the email.
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@local
ADMIN_PASSWORD=

# Set to 'true' when serving over HTTPS so the session cookie is only sent over
# TLS (recommended for any public/network deployment). Default off for local dev.
COOKIE_SECURE=false

# Session lifetime in days (how long a login stays valid). Default 30.
# SESSION_TTL_DAYS=30

# Email is NOT yet wired up: registration email-confirmation and the
# "forgot password" email link are inert. Password resets are admin-initiated
# (the user list has a "Reset password" action that produces a one-time link).
